# Only the backend URL is needed at build time.
# All other config (Stripe key, chat URL, Novu App ID) is fetched at runtime
# from GET /api/public/config — no rebuild needed when these change.
EXPO_PUBLIC_API_URL=http://localhost:3000
